# 4.2.12

# Features: 
- Configurable footerMessage. 
  You can now use the `footerMessage` setting in the main `.properties` file to customize the HTML in the page footer. 
  Similarly to the `bannerMessage` setting.

# Fixes: 
- Fix findhit parameter in article page not navigating to the correct hit
- Add api=4 to /relations request as well.
