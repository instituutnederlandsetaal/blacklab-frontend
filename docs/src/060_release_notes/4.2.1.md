# 4.2.1

# Improvements: 
- Implement caching and etag support for static files and some specific API requests, this should improve initial page load times significantly.
- Support for range requests on static files to enable partial content loading.
- Dropdown menus (selectpickers) will now always be searchable when they have many options
