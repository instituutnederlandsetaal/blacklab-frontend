We put things here that instantiate globals for use in the app.
The globals can generally be consumed by using the appropriate `use*` composable.
The `use*` uses the provide/inject mechanism to get the global (app-scoped) instance.
