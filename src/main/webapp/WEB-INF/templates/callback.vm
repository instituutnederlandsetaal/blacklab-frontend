<html>
<head>
    <meta charset="utf-8">

    <script>
        var CONTEXT_URL = "$esc.javascript($CF_URL_ON_CLIENT)";
        var OIDC_AUTHORITY = "$esc.javascript($OIDC_AUTHORITY)";
        var OIDC_METADATA_URL = "$esc.javascript($OIDC_METADATA_URL)";
        var OIDC_CLIENT_ID = "$esc.javascript($OIDC_CLIENT_ID)";
        var WITH_CREDENTIALS = false; // must be defined for api to initialize.
    </script>

    #if($VITE)
    <script type="module" src="$VITE/@vite/client"></script>
    <script type="module" src="$VITE/src/callback.ts"></script>
    #else
    <script src="$CF_URL_ON_CLIENT/js/callback.js?_$cache"></script>
    #end
</head>
<body>
    <div class="container">
        Callback for oidc login process.
    </div>
</body>
</html>