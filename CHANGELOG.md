# Features: 
- Allow span attributes to be included in CSV export via custom.js

# Fixes: 
- Fix broken URL decode of custom filters
- Fix removeLocale not removing the locale from the selector dropdown
